1- make every string mentioned is localized for en and ar
2- maker every widget added is strucured and if have omore than one cane be reauseble reuse it as component widget
3- use fon asome library and makey every single thing is iconed and make ui is viziulized deeply use icons form font awsome only
4- use cairo font as main font at the project
5- make ci cd like dash dashboard upload directly when merge with main
- retrive every point at main plan and my prompt is following nad make sure the current is a clickable with is